{
  "name": "bostonpolisci26",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:e2e": "playwright test --project=mobile --project=desktop",
    "freeze": "playwright test --project=visual-light --project=visual-dark",
    "freeze:update": "playwright test --project=visual-light --project=visual-dark --update-snapshots",
    "freeze:docker": "docker run --rm --network host -u \"$(id -u):$(id -g)\" -e HOME=/tmp -v \"$PWD/..\":/repo -w /repo/app mcr.microsoft.com/playwright:v1.62.1-noble npx playwright test --project=visual-light --project=visual-dark",
    "freeze:docker:update": "docker run --rm --network host -u \"$(id -u):$(id -g)\" -e HOME=/tmp -v \"$PWD/..\":/repo -w /repo/app mcr.microsoft.com/playwright:v1.62.1-noble npx playwright test --project=visual-light --project=visual-dark --update-snapshots"
  },
  "dependencies": {
    "fuse.js": "^7.5.0",
    "qrcode.react": "^4.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
